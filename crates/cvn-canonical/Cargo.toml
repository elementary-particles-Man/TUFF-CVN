[package]
name = "cvn-canonical"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
cvn-core = { path = "../cvn-core" }
cvn-schema = { path = "../cvn-schema" }

[lib]
path = "src/lib.rs"
